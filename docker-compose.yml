web:
  build: .
  working_dir: /allura
  command: paster serve Allura/docker-dev.ini --reload
  ports:
    - "8080:8080"
  volumes:
    - .:/allura
  links:
    - mongo
mongo:
  image: mongo:2.6
  expose:
    - "27017"
  volumes:
    - /allura-data/mongo:/data/db
